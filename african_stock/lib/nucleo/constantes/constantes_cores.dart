import 'package:flutter/material.dart';

class CoresApp {
  // --- IDENTIDADE ---
  static const Color primaria = Color(0xFF137FEC);
  static const Color primariaEscura = Color(0xFF0D5BB5);
  static const Color acento = Color(0xFF00D4FF);

  // --- STATUS ---
  static const Color sucesso = Color(0xFF10B981);
  static const Color erro = Color(0xFFEF4444);
  static const Color alerta = Color(0xFFF59E0B);
  static const Color info = Color(0xFF3B82F6);

  // --- COMPATIBILIDADE (Para corrigir os teus erros de compilação) ---
  // Estes nomes são os que as tuas telas (Login, Dashboard) estão a pedir
  static const Color textoPrincipal = Color(0xFF0F172A);
  static const Color textoSecundario = Color(0xFF64748B);
  static const Color borda = Color(0xFFE2E8F0);

  // --- MODO CLARO (LIGHT) ---
  static const Color fundoClaro = Color(0xFFF6F7F8);
  static const Color cardClaro = Colors.white;
  static const Color textoClaroPrincipal = Color(0xFF0D141B);
  static const Color textoClaroSecundario = Color(0xFF64748B);
  static const Color bordaClaro = Color(0xFFE2E8F0);

  // --- MODO ESCURO (DARK) ---
  static const Color fundoEscuro = Color(0xFF101922);
  static const Color cardEscuro = Color(0xFF1E293B);
  static const Color textoEscuroPrincipal = Colors.white;
  static const Color textoEscuroSecundario = Color(0xFF94A3B8);
  static const Color bordaEscuro = Color(0xFF334155);
}